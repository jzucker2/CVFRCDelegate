project 'Example/CVFRCDelegate'
workspace 'CVFRCDelegate'
use_frameworks!

target 'CVFRCDelegate_Example' do
  platform :ios, '10.0'
  pod 'CVFRCDelegate', :path => '.'

  target 'CVFRCDelegate_Tests' do
    inherit! :search_paths

    
  end
end
